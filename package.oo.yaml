icon: https://oomol-flows.oss-cn-hangzhou.aliyuncs.com/1724146494_agriculture.png
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    pnpm install && pip install -r requirements.txt
dependencies:
  oomol-file: 0.0.4
name: climate-disaster-stats
version: 0.0.3
description: agricultural analysis
displayName: Climate Disaster Stats
repository: https://github.com/oomol-flows/climate-disaster-stats
releaseNotes: update version and add blocks
